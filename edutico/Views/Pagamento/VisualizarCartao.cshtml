@{
    Layout = "_LayoutCli2";
    ViewData["Title"] = "Cartão Pagamento";
}

@model Cartao;

<section class="alinhamento">
    <div class="text-center texto-simples">
        <h5 class="py-3 glacial-bold mb-5">Visualize o seu cartão</h5>
        <div class="base-cartao">
            <div class="d-flex mb-4">
                <p><img src="~/imgs/Bandeira.png" alt="bandeira" class="bandeira"> Cartão</p>
            </div>
            <div class="d-flex w-50 justify-content-between mb-5">
                <p>****</p>
                <p>****</p>
                <p>****</p>
                <p>@Model.numCartao.ToString().Substring(Model.numCartao.ToString().Length - 4)</p>
            </div>
            <p class="text-esquerda">Data de validade: @Model.dataVali</p>
        </div>
        <form asp-controller="Home" asp-action="CartaoPagamento" method="post">
            <button class="btnnav">
                Voltar
            </button>
        </form>

    </div>
</section>


